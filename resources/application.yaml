logging:
  level: TRACE

server:
  port: 5015

application:
  name: hangout-content-delivery-api

aws:
  region:
  image:
    s3:
      bucket:
      expiration_duration_seconds:
  video:
    cloudfront:
      private_key_path:
      public_key_id:
      domain:
      expiration_duration_seconds:

cookie:
  domain:      

otel:
  endpoint:
